# Ретроспектива и мониторинг оркестрации

Назначение файла — живой лог наблюдений за мультиагентной оркестрацией в этом репозитории:
производительность и сбои каналов, нюансы механизма делегирования, что улучшить в следующих фан-аутах.
Ведётся под ТЕКУЩИЙ стек делегирования; правила и приоритеты — в `CLAUDE.md`, раздел «Делегирование
субагентам».

---

## Текущий стек каналов

| Канал | Роль | Сильные стороны |
|-------|------|-----------------|
| **Kiro CLI** (MCP `kiro-cli`, первый приоритет) | нативный Kiro-агент: правит файлы в `workdir`, сессия на диске (`delegate`/`reply`), max reasoning | объёмные правки кода, второе мнение по дизайну; модели `claude-opus-4.8` → `claude-sonnet-4.6` → `glm-5` |
| **Codex** (MCP, второй приоритет) | глубокий анализ/ресёрч, чтение реального репозитория | внешне-API-фезибилити, «посчитай по реальному коду», адверсариальная критика |
| **Claude-Agent / Workflow** | свои Task/Agent-субагенты, структурный fan-out | фронт (FSD-слайсы), самопроверка eslint/vue-tsc, structured output для синтеза |
| **Главная модель** | оркестратор | сводит контракты, верифицирует чужие diff, держит когерентность по `CLAUDE.md` |

Параллельность kiro-cli (обновлено 2026-06-24): обёртка стала async, прежний СИНХРОННЫЙ боттлнек снят —
допускает до `KIRO_MAX_CONCURRENCY` (дефолт 3). На РАЗНЫХ аккаунтах/worktree параллель реальная (до
`min(3, k)`); на ОДНОМ аккаунте 3 одновременных ПОДТВЕРЖДЕНЫ живым тестом 2026-06-24 (3 модели разом,
все exit=0, без throttle; прежняя «сериализация» была из-за обёртки, не аккаунта; см. `playbook.md` §6). Backend
per-account spacing/429 — вторичный лимит (throttle-guard). Параллель по умолчанию ВЫКЛ, включается явной
командой; больше 3 — разводить по РАЗНЫМ каналам/директориям.

---

## Durable-уроки (channel-agnostic, из сборки блока «Автоматизация»)

1. **Замораживать ТОЧНЫЕ контракты ДО fan-out** — не только имена ресурсов, а точные пути/методы/формат
   ответа. Рассинхрон путей (`/automation/tasks/{id}/parse` vs `/automation/{id}/parse-targets`) и
   формата ответа (`data:{task,parse_run}` vs плоско) пришлось сводить руками, и они всплыли как живые
   500-е. Pre-flight «shared decisions» одним коротким решением срезает половину работы по сведению.
2. **Обязательная фаза Critique.** Адверсариальный критик поймал блокеры, которые не увидел никто:
   `auth()->id()` внутри клиента (в очереди `null→0`, ломает ownership/лимиты), отсутствие
   scheduler-контейнера, конфликт драйвера очереди. Без неё — красивый, но нерабочий дизайн.
3. **Партиционировать fan-out по непересекающимся файлам/каналам/директориям.** Два агента на одну зону
   расходятся по несущим решениям → дорого сводить. Либо один агент на зону, либо перекрытие делать
   ЯВНО адверсариальным (один проектирует, второй атакует). Конкурентная правка общего дерева работает
   без worktree, если скоупы заданы непересекающимися явно.
4. **Маршрутизация по сильным сторонам каналов** (подтверждено): внешне-API-фезибилити + «посчитай по
   коду» → Codex; адверсариальная критика архитектуры → in-house критик; независимое второе мнение по
   дизайну / объёмная правка → Kiro; фронт + самопроверка → Claude-Agent.
5. **Живая проверка ловит то, что типы/тесты не ловят.** Реальный прогон вскрыл 4 бага, невидимых для
   тестов: рассинхрон формата ответа store↔бэк, broadcast «payload too large» роняет job, реактивный
   дедлок `accountId`, авто-refresh review после async-парсинга. Гонять вживую, не только зелёные гейты.
6. **Агенты выходят за объявленный скоуп** (правят routes/provider, которые просили не трогать) И
   пропускают часть. Always verify: что сделано лишнего и что НЕ сделано. Швы (роуты, каналы, schedule,
   биндинги) сводит оркестратор последовательно после слияния слайсов.
7. **Инфра-грабли:** `queue:work` кэширует код при старте — после правок рестартить ВСЕ воркеры
   (queue-worker + automation-worker), иначе job падает на старом коде.
8. **Не предполагать «исправлено/разблокировано» без теста.** 2 неудачи подряд = не транзиент, не
   долбить. И сверять с фактическим `now()` ДО вывода о TZ-баге (длинная сессия — реальное время ушло).

---

## Мониторинг механизма `kiro-cli` (живой лог)

Сюда — наблюдения по каждому заметному прогону `kiro-cli`: модель/effort, объём, сбои, нюансы вывода.

- **2026-06-21 — первый боевой прогон нового `kiro-cli`** (перевод `agent-factory.md` на актуальный
  механизм делегирования, `delegate` + `reply`, `claude-opus-4.8` / `max`). Итог: **работает** — обе
  итерации правок применены корректно, файл независимо проверен grep'ом (мёртвая связка вычищена,
  дублей нет). Нюансы вывода обёртки для будущих прогонов:
  - `reply` вернул баннер `ОШИБКА: Kiro не аутентифицирован`, хотя `exit=0` и правки легли. **Корень —
    ложноположительная проверка обёртки:** `_auth_failed` искала подстроку «kiro-cli login» по всему
    выводу, а та была в КОНТЕНТЕ (правки про «профили `kiro-cli login`»). **Пофикшено**
    (`kiro-cli-mcp/kiro_runner.py`: auth-сбой засчитывается только при ненулевом exit; нужен реконнект MCP).
  - В конце `reply` — повторный edit уже заменённого блока с `Execution failed: no occurrences` (тоже
    безвредно: идемпотентная попытка на отсутствующем тексте).
  - **Урок:** итог `kiro-cli` сверять ФАКТИЧЕСКИМ состоянием файла (grep/diff/чтение), а не текстовым
    summary обёртки — summary может содержать шум/противоречия. Это штатная верификация делегата.
  - Сильная сторона: агент сам поймал остаток мёртвой механики БЕЗ ключевого слова (абзацы про
    маршрутизатор/привязку в §5.5, которые узкий grep пропустил) и предложил доработку — полезно.

- **2026-06-21 — серия: реструктура доков + аудит code-references** (`delegate`/`reply`,
  `claude-opus-4.8`). Итог: **работа корректна** (паутина `CLAUDE.md`; перенос корневых доков в `docs/`
  с группами и починкой ссылок; сверка FSD/app-дерева с реальным кодом — всё независимо проверено
  grep/diff/ls). Два сбоя обёртки/самоотчёта:
  - **`reply` упал с `can only concatenate str (not "bytes") to str`** (TypeError обёртки, не контент —
    прочие `reply`/`delegate` в той же серии работали). Обойдено свежим `delegate` (другой код-путь) —
    отработал штатно. Кандидат на фикс в `kiro-cli-mcp`: где-то bytes-вывод конкатенируется к str-промпту.
  - **Самоотчёт разошёлся с фактом:** в аудите Kiro заявил «правил ровно одну строку», а `git diff` —
    43 правки в 4 файлах (все КОРРЕКТНЫЕ); многофайловый diff ошибочно списал на «незакоммиченные правки
    прошлой сессии» (а они были закоммичены, дерево было чистым). Поймано сверкой `git diff HEAD`.
    **Урок (повтор): у Kiro принимать ТОЛЬКО факт-diff, summary не доверять — он бывает внутренне
    противоречив даже когда сама работа верна.**

- **2026-06-21 — фикс ActionScheduler (дубли media) + регресс-тест** (`delegate`, `claude-sonnet-4.6`).
  Контекст: дебаг «задача висит/падает» в Automation. Делегирован независимый ревью фикса
  `ActionSchedulerService::scheduleTask` (insertOrIgnore + отсев уже заактионенных media) и написание
  регресс-теста. Итог: **работа корректна** — Kiro верно разобрал edge-cases (media_pk=null,
  cancelled-слоты unique, lockForUpdate-гонки), создал `tests/Unit/Services/ActionSchedulerServiceTest.php`
  (RefreshDatabase + DataProvider по конвенциям), 5 passed. Тест перепроверен независимо — зелёный.
  Нюансы канала:
  - **Кракозябры в выводе:** текстовый итог `delegate` вернулся как UTF-8, декодированный в Latin-1
    (кириллица → `Ð…`). На файлы/diff не влияет (тест корректен), но человекочитаемый отчёт в
    tool-result нечитаем. Кандидат на фикс кодировки stdout в `kiro-cli-mcp`.
  - **Побочный артефакт:** `delegate` создал в workdir `.kiro/settings/lsp.json` (IDE-конфиг Kiro) —
    удалён как непредусмотренный мусор; стоит добавить в `.gitignore` или чистить в обёртке.

- **2026-06-21 — фронтовый UX-фикс Automation (4 бага)** (`delegate`, `claude-opus-4.8`, workdir
  `frontend-vue`). Задача: кликабельная карточка задачи → возврат в ревью; снять вечную «Загрузку…»
  при 0 целях; `fetchTasks` после запуска; пустое состояние «Завершена 0/0». Итог: **корректно** —
  3 файла (`AutomationPage.vue`, `AutomationTaskList.vue`, `useParseProgress.ts`), eslint чисто,
  vue-tsc без новых ошибок (осталась только предсуществующая в `quasar.config.ts`). Diff перепроверен
  чтением + живым прогоном в браузере (клик по черновику открывает таблицу; статус draft→Планирование→
  Выполняется обновляется realtime; «0/0» заменён осмысленным текстом). Кракозябры в текстовом отчёте
  снова присутствовали (UTF-8→Latin-1), но на файлы не влияют — сверка по git-diff и факту. На этот раз
  стороннего `.kiro/`-артефакта не возникло. **Вывод: opus-4.8 на узко-сформулированной много-файловой
  фронт-задаче с явными file:line отработал чисто.**

- **2026-06-21 — UX-фикс карточки задачи + тесты (Codex-аудит → Kiro Opus → Kiro Sonnet)**. Полный
  трёхканальный конвейер на одной фиче:
  - **Codex** (`read-only`, `priority`/`high`): глубокий аудит причин (парс-прогресс льётся в карточку
    исполнения через `applyProgress`; `current_action='like'` в терминальном событии; Stop у draft) +
    аудит покрытия тестами. Дал точную карту file:line и спецификацию фиксов/тестов — независимо
    подтвердил предварительный анализ оркестратора. Сильный канал для «разберись по реальному коду».
  - **Kiro Opus**: фиксы отображения (фильтр parsing-событий, `canCancelStatus`, `ACTION_TYPE_LABELS`,
    `currentActionLabel`) — чисто, eslint/vue-tsc ок.
  - **Kiro Sonnet**: регресс-тесты (FE 4 файла + BE 1). Прогон зелёный, НО верификация фактом вскрыла
    два дефекта качества, которые vitest не ловит: (1) **тавтология** — `currentActionLabel`/
    `emptyTerminalText` были СКОПИРОВАНЫ в тест-файл (объявлены внутри `<script setup>` SFC, не
    экспортировались). Починено вынесением хелперов в `taskStatusMeta.ts` + импортом из реального
    кода. (2) **type-ошибка мока** (`mode: 'auto'` — невалидный `AutomationMode`): `vitest run`
    проходил без typecheck, а `vue-tsc` падал. Починено.
  - **Урок:** у делегированных тестов проверять не только «зелёный прогон», но и (а) `vue-tsc`/типы
    (vitest их не видит), и (б) что тест бьёт по РЕАЛЬНОМУ коду, а не по локальной копии логики
    (частый failure-mode при тестировании функций, спрятанных в `<script setup>` — выносить в lib).

- **2026-06-22 — live-check harness (Playwright) + фикс счётчика черновика** (`delegate`, `claude-opus-4.8`).
  - **Создан и проверен headless-Playwright live-check** (`frontend-vue/scripts/live-check/automation-live.mjs`):
    открывает ЖИВОЙ сайт на хосте, токен из `localStorage`, ловит реальные API/WebSocket/цели — без моков.
    **Триал-сверка 1/10:** оркестратор сам перезапустил скрипт — данные совпали (реальная задача #30, кадры
    `AutomationTaskProgress`, реальные цели @feniiichka/@turkasova). Нюанс: запускать ТОЛЬКО на хосте (в
    контейнере `vue` WS до Reverb:8080 недостижим). Правило закреплено в корневом `CLAUDE.md` (раздел
    «Проверка UI / визуальные регрессии») + memory `feedback-ui-verification-via-playwright`.
  - **Фикс счётчика черновика**: карточка показывала «Всего:0/Готово:0», хотя цели спарсены. Добавлено поле
    контракта `collected_targets_count` (BE подзапрос-scope, без N+1) → `collectedTargetsCount` (FE DTO) →
    хелпер `taskStatsKind` (draft→«Собрано целей:N»). Kiro Opus; верифицировано фактом (BE 5 + FE-тесты,
    реальный API `collected_targets_count:2`, визуально #30→«Собрано целей:2»). Кросс-чек совпал — расхождений нет.

- **2026-06-22 — таймлайн запуска (F1) + skip уже-лайкнутых при парсинге (F2)** (ПАРАЛЛЕЛЬНО по разным
  каналам, файлы непересекающиеся: **Kiro Opus → F1**, **Codex workspace-write → F2**).
  - F1: интерактивный draggable-таймлайн (Quasar `v-touch-pan`) + контракт `/start` с пер-целевыми
    `offset_seconds` + планировщик `buildItemsWithOffsets`. Kiro Opus **ТАЙМАУТНУЛ 600с** (код дописан, но
    без прогона/самопроверки) → оркестратор добил: vue-tsc-ошибка `exactOptionalPropertyTypes` в
    `startTask`, eslint-warnings, кривой формат `spacingSeconds`. После фикса зелёное (vue-tsc, vitest 368, BE 246).
  - F2: `ParseTargetsJob` — skip `has_liked` + already-actioned (`automation_action_items`) + добор страниц
    до `target_limit`. `has_liked` уже приходит из Python (менять не нужно). **Codex не смог `docker compose
    exec`** (нет доступа к docker-сокету из песочницы) — гонял `php artisan test` на хосте; перепроверено в контейнере.
  - **Тест через headless Playwright** (`launch-timeline-check.mjs`): drag кубика 0→1 мин, контракт `/start`
    с offsets отправлен — F1 доказан в UI без реального браузера.
  - **КЛЮЧЕВОЙ УРОК (поймала live-проверка):** первый live-прогон ПРОВАЛИЛСЯ — offsets не применились, F2 не
    сработал (collisions=2). Причина: **`queue:work`-демоны кэшируют код**, воркеры не перезапущены после
    правок Jobs/Services. Тесты (свежий код) это маскировали. После `restart queue-worker automation-worker`
    повтор: run_at=60/1800/3600с, F2 collisions=0. **Правило закреплено в корневом `CLAUDE.md`.** Триал-сверка 2/10.

- **2026-06-22 — pre-commit верификация всей партии (Workflow 4 агента + Kiro Opus аудит + Kiro Opus тест)**.
  Перед коммитом — мультиагентная проверка «двигаются ли кубики / сделано ли до конца»:
  - **Workflow (4 параллельных агента, structured output):** FE-статика (vitest 381/381, eslint чисто;
    vue-tsc — 2 предсуществующие ошибки ВНЕ диффа: `quasar.config.ts`, `TableComponent.spec.ts`; нового
    tsc-долга нет), BE-тесты (246 pass, 1 skip — PG `::date`), live-UI на живом стеке (реальный mouse-drag
    кубика 0→2 мин, окно зажалось до 5 мин, 6 кубов distinct, console-ошибок 0, скриншоты), аудит
    полноты (claude) — «complete».
  - **Kiro Opus аудит (max)** независимо: «complete, блокеров нет» — контракт offsets сквозной (ключ
    `parsed_target_id`), дедуп 2-слойный, has_liked-skip ок, MIN_GAP/snap согласованы FE↔BE.
  - **Закрыт реальный пробел:** оба аудита нашли, что `buildItemsWithOffsets` (ядро F1) не имел BE-unit
    покрытия (только live). Kiro Opus дописал 4 теста (run_at=t0+offset по id; MIN_GAP-раздвижка; игнор
    jitter; дедуп+offset-by-id) → оркестратор прогнал в docker: **9/9 PASS, 23 assertions**.
  - **Доки выровнены:** §8.2/§11 архитектуры — `time-distribution-preview` → `time-distribution-editor`
    (интерактивный), добавлен пост-релизный инкремент-блок.
  - Кракозябры Kiro в тексте снова были — файл-тест корректен, сверка фактом (чтение + docker-прогон).
    Триал-сверка live-check 3/10: BE/FE-тесты перепроверены независимым прогоном; live-drag принят по
    артефактам агента (скриншоты + монотонные offsets + 0 console-ошибок) без повторного прогона.

- **2026-06-22 — тест механики делегирования (хук + Kiro + Codex) на проверке последних коммитов.**
  - **Хук `guard-delegation.py`:** РАБОТАЕТ — `Write` в `frontend-vue/src/pages/` физически заблокирован
    (`deny` с инструкцией делегировать). Enforcement активен в сессии, ручная активация не нужна.
  - **Тесты (оркестратор сам):** BE 250 pass / Py 176 pass / FE 381 pass — всё зелёное.
  - **Kiro live-check (sonnet) — OK:** прогнал `automation-live.mjs`, вернул отчёт. Триал-сверка 4/10 —
    параллельный прогон оркестратора сошёлся 1:1 (feasible=ДА, оба статуса 200, те же 2 цели `kept`).
  - **Codex review (priority/high) — OK, 4 находки, ВСЕ подтверждены фактом оркестратором:** #1
    reserve-до-claim → недосчёт лимита дублем job (major); #2 дубли follow/unfollow (null `media_pk`
    обходит unique+дедуп, major); #3 `useParseProgress` 0-целей висит 120с (major UX); #4
    `latestActivityLogId` чужой лог (minor). Codex-канал дал реальную ценность — находки точные.
  - **⚠️ СБОЙ Kiro (opus-4.8) — диагноз УТОЧНЁН ДВАЖДЫ (важный мета-урок про верификацию):**
    финальная картина по Codex (xhigh, построчно) + проверке `StatusCode()`: **52× HTTP 200, НИ ОДНОГО
    реального 429/500/503, 0 retry attempt=2+** — реального rate-limit/throttling НЕ БЫЛО. Падение —
    разовый event-stream `InternalServerError` бэкенда Kiro («Kiro is having trouble responding right
    now»), транзиент; аккаунт жив (sonnet сразу вернул `ok`).
    • Ложный «rate-limit storm» (первая оценка) — артефакт grep по ПОДСТРОКАМ trace, в который подмешан
      ТЕКСТ САМОЙ ЗАДАЧИ (она была про класс `RateLimitGuardService`!) + SDK-список `retry_classifiers`.
    • Ложный «не аутентифицирован» — `_auth_failed` ловил optional Builder ID `not logged in`.
    **Мета-урок: НЕ диагностировать по grep-подстрокам trace с подмешанным собственным промптом; HTTP-коды
    смотреть строго по `StatusCode(N)`. Два неверных вывода подряд поймал только построчный анализ Codex.**
  - **Фиксы обёртки `kiro-cli-mcp` (этот заход), ПРИМЕНЕНЫ и верифицированы:** (оркестратор сам)
    `_decode_buf`→`json.loads` (кракозябры в debug убраны, кириллица цела; debug ОСТАВЛЕН включённым);
    `_auth_failed` не матчит optional `not logged in`/`kiro-cli login` (убран ложный auth); `_kiro_env` —
    HTTPS_PROXY единым маршрутом, socks `ALL_PROXY` отключён (геолокация не скачет), анти-бан guard цел.
    (Codex workspace-write + verify оркестратором) `_redact_secrets` маскирует auth-заголовки/JWT/токены
    перед записью debug-trace (security); throttle-guard — `_rate_limited` (строгие маркеры, НЕ ложит на
    `RateLimitGuardService`/SDK-classifiers/голый 429) + persistent `logs/throttle-state.json` (flock+atomic)
    + preflight cooldown ДО сети + per-model circuit-breaker (env `KIRO_THROTTLE_*`). Verify: import + smoke
    + интеграция вызовов (preflight до `_run_chat`, `_rate_limited` до `_auth_failed`). **Активируется после
    перезапуска MCP-сервера kiro-cli** (модуль в памяти долгоживущего процесса).
  - **✅ ПРОВЕРЕНО ВЖИВУЮ после перезапуска MCP (2026-06-22):** delegate `claude-opus-4.8`/`max` → `exit=0`,
    кириллица в summary чистая (кракозябры ушли), лог `proxy: source=HTTPS_PROXY (единый маршрут, socks
    ALL_PROXY отключён) endpoint=127.0.0.1:8118`, рассинхрон-WARNING исчез, ложный auth не сработал. Сбой
    opus был разовым транзиентом — opus/max возвращён как основной делегат для тяжёлых задач (throttle-guard
    страхует от реального rate-limit, которого пока не было).

---

## Что улучшить в следующей оркестрации

1. **Pre-flight «shared decisions» ДО fan-out** — канонические имена/пути/контракты одним решением.
2. **Острее партиционировать** — не два независимых дизайна одной зоны, а явная адверсариальность.
3. **Сохранять фазу Critique** — окупается (ловит блокеры).
4. **Structured output (schema) в Workflow** — упрощает синтез, дефолт для сводимого fan-out.
5. **Заземление перед fan-out** (чтения карты слоёв в main-контексте) — промпты ссылаются на реальные
   файлы/классы, агенты не плутают.
6. **Верификация делегатов — обязательна:** каждый diff/итог проверять самому (grep/тесты/чтение),
   черновики чужих моделей не принимать на веру.
